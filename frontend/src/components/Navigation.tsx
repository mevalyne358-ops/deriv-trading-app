import React from 'react';\nimport { useNavigate } from 'react-router-dom';\nimport { useStore } from '../store/useStore';\n\nexport const Navigation: React.FC = () => {\n  const navigate = useNavigate();\n  const { isAuthenticated, isDemoMode, toggleDemoMode, logout } = useStore();\n\n  if (!isAuthenticated) {\n    return null;\n  }\n\n  return (\n    <nav className=\"bg-gray-900 text-white shadow-lg\">\n      <div className=\"max-w-7xl mx-auto px-4 py-4 flex items-center justify-between\">\n        <div className=\"flex items-center gap-8\">\n          <h1 className=\"text-2xl font-bold cursor-pointer\" onClick={() => navigate('/trading')}>\n            📈 Deriv Trading\n          </h1>\n          <div className=\"flex gap-4\">\n            <button\n              onClick={() => navigate('/trading')}\n              className=\"hover:text-blue-400 transition font-semibold\"\n            >\n              Trading\n            </button>\n            <button\n              onClick={() => navigate('/portfolio')}\n              className=\"hover:text-blue-400 transition font-semibold\"\n            >\n              Portfolio\n            </button>\n          </div>\n        </div>\n\n        <div className=\"flex items-center gap-4\">\n          <button\n            onClick={toggleDemoMode}\n            className={`px-4 py-2 rounded font-semibold transition ${\n              isDemoMode\n                ? 'bg-yellow-500 text-white hover:bg-yellow-600'\n                : 'bg-red-500 text-white hover:bg-red-600'\n            }`}\n          >\n            {isDemoMode ? '🎮 Demo Mode' : '💰 Live Mode'}\n          </button>\n\n          <button\n            onClick={() => {\n              logout();\n              navigate('/login');\n            }}\n            className=\"px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded font-semibold transition\"\n          >\n            Logout\n          </button>\n        </div>\n      </div>\n    </nav>\n  );\n};\n